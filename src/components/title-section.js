import * as React from "react";

const TitleSection = ({ children }) => {
  return (
    <h2 class="text-4xl font-bold text-center col-span-2 text-gray-800">{children}</h2>
  );
};

export default TitleSection;
