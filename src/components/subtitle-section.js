import * as React from "react";

const SubtitleSection = ({ children }) => {
  return (
    <h3 className="text-center uppercase text-purple">{ children }</h3>
  );
};

export default SubtitleSection;
